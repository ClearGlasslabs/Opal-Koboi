# UI Domain

The production Next.js command surface lives in `apps/web`. This boundary owns accessible glassmorphism components, secure action modals, high-contrast visualizations, and typed API clients; it contains no authorization logic.
