# Threat Intelligence

Connector adapters normalize signed external feeds into `intelligence.observed.v1` events. Connectors run read-only, validate source provenance, apply per-tenant quotas, quarantine malformed records, and never execute indicators.
